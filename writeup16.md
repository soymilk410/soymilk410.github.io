# Bugku CTF 矛盾 Writeup

## 题目信息
来源:Bugku CTF

类型:WEB

## 心路历程
打开题目为PHP代码 解析意思

is_numeric("123") → ✅返回 true，认定是数字

is_numeric("1a") → ❌返回 false，因为里面有字母 a，整体不是纯数字字符串

!is_numeric($num) 前面的感叹号!是取反。需要没有数字

PHP 做==对比的时候，两边类型不一样，会自动把字符串强行转成数字再比较！

举例子：
"1abc" 和数字1用 == 比较 PHP 会读取开头1，后面 abc 直接忽略。于是："1abc" == 1 → 结果是 true

条件一说整体不能是纯数字；
条件二又要它弱比较等于 1。于是扣题发现这就是名字的来源

于是用浏览器地址栏 GET 传参 在网址输入?num=1flag 最终成功得到


## 收获
is_numeric() 函数用来判断内容是不是纯数字，带字母就判定不是数字。

PHP 的==弱比较会自动转换数据类型，存在逻辑漏洞；===强比较会同时检查值和类型，更安全，写代码优先用强比较。

GET 传参直接在网址后面加?参数名=内容，就可以给网页提交数据测试。

字符串和数字弱比较时，PHP 只会读取字符串开头的数字，遇到字母就停止读取。

flag{3d0517d39d2cc9142c27d1d1ccfe13cf}
